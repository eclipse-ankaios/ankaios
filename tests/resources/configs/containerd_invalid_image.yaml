apiVersion: v0.1
workloads:
  invalid_image_workload:
    runtime: containerd
    agent: agent_A
    restartPolicy: NEVER
    tags:
      - key: owner
        value: Ankaios team
    runtimeConfig: |
      image: invalid-image
